#include <stdio.h>
void  main() {
             char A;
              scanf("%c",&A);
              if (A=='A'|| A=='e'|| A== 'a' || A=='i' ||A=='o' ||A=='u')
              {
                printf("cons is a vowel");
              }
              else
              {
                printf("cons is a consonant");
              }
              
}