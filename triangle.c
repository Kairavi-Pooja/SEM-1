#include <stdio.h>
void main() {
             int base;
             scanf("%d",&base);
             int height;
             scanf("%d",&height);
             float area;
             area = 0.5*(float)base*height;
             printf("%0.1f",area);
            }